{
  "name": "fledge-plugin-memory",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile bin/fledge-memory",
    "test": "bun test"
  },
  "dependencies": {
    "@corvidlabs/ts-algochat": "^0.4.0",
    "algosdk": "^3.5.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
