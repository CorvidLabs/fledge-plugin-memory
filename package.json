{
  "name": "fledge-plugin-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile bin/fledge-memory",
    "test": "bun test"
  },
  "dependencies": {
    "algosdk": "^3.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
